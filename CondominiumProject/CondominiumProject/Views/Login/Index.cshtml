
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="~/css/StyleLogin.css" asp-append-version="true" />
</head>
<body>
    <div class="container">
        <div class="row g-0 text-center">
            <div class="logincard">
                <div class="card" style="width: 18rem;">
                    <form asp-controller="Login" asp-action="Login">
                        <br>
                        <h4>Condominun APP</h4>
                        <img class="imgcard" src="~/AppImages/ImagePrincipal.jpeg" class="card-img-top" alt="...">
                        <div class="card-body">
                            <div class="form-floating mb-3">
                                <input name="email" type="email" class="form-control" id="floatingInput" placeholder="name@example.com" required>
                                <label for="floatingInput">Email addres</label>
                            </div>
                            <div class="form-floating">
                                <input name="password" type="password" class="form-control" id="floatingPassword" placeholder="Password" required>
                                <label for="floatingPassword">Password</label>
                            </div>
                            <br>
                            <div id="fecha"></div>
                        </div>
                        <div class="card-footer">
                            <button type="submit" class="btn btn-success">Iniciar Secion</button>
                        </div>
                    </form>
                </div>
            </div>


        </div>
    </div>

    <script>
        const fechaActual = new Date();

        // Obtener el año actual
        const opciones = { year: 'numeric' };
        const añoActual = fechaActual.toLocaleDateString('es-ES', opciones);

        // Mostrar la fecha en el elemento HTML en el formato deseado
        const elementoFecha = document.getElementById('fecha');
        elementoFecha.textContent = `CondominumAPP ${añoActual}`;
    </script>

</body>
</html>
